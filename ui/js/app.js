/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */

let twoSum = function(nums, target) {
    let result = [];
    let hash = {};
    for (let i=0; i<nums.length;i++) {
        hash.nums[i] = nums[i];
    }
    Object.values();
    return result;
};